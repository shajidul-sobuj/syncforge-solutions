#include <bits/stdc++.h>using namespace std;#define ll long long#define endl '\n'// cont int N = 1e5;   void boom(){     int n; cin >> n;    int sum = 0;    for (int i=0;i<n;i++){        int x,y,z; cin >> x >> y >> z;        int t = (x+y+z);        if(t>=2) sum++;    }    cout << sum << endl;      }     int main() {    ios_base::sync_with_stdio(false);    cin.tie(nullptr);     int t = 1;    // cin >> t;    while(t--)boom();    return 0;}